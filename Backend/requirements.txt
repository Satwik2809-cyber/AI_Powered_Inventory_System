# Core framework
fastapi
uvicorn[standard]

# Database & ORM
sqlmodel
sqlalchemy
psycopg2-binary

# Authentication & Security
python-jose[cryptography]
passlib[bcrypt]

# Environment & Config
python-dotenv

# Date & Time
pytz

# File handling
python-multipart

# Validation
pydantic

# Excel / Data
pandas
openpyxl

# Fuzzy matching (AI commands)
thefuzz
python-Levenshtein

# Imaging
pillow
imagehash

# NOTE: torch, torchvision, easyocr are intentionally excluded from production.
# They are very large (2-5 GB) and will cause Render build to time out / fail.
# image_match.py gracefully degrades if torch is not installed.
